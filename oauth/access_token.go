package oauth
/*
import (
    "errors"

    "github.com/earaujoassis/space/utils"
    "github.com/earaujoassis/space/datastore"
    "github.com/earaujoassis/space/models"
)

func AccessToken(data utils.H) (*models.Session, error) {
    var scope string = data["scope"]
    var grantType string = data["grant_type"]
    var code string = data["code"]
    var clientId string = data["client_id"]
    var clientSecret string = data["client_secret"]

    if scope == "" || grantType == "" || code == "" || clientId == "" {
        c.String(http.StatusMethodNotAllowed, "Missing required parameters")
        return
    }
    return nil, nil
}
*/
