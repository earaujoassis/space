#!/bin/bash

set -xe

git config core.hooksPath hooks
npm install && npm run build

if [ ! -f .env ]; then
  cp .env.sample .env
  echo ".env file created; please fill it with the details"
fi

echo ""
echo "Setup completed. You may run 'go run main.go serve' to start the server"
